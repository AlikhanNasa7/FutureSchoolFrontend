/**
 * API service functions for Resources
 */

import axiosInstance from '@/lib/axios';
import type { Resource } from '@/types/course';

export const resourceService = {
    // Resource service functions can be added here if needed
};
